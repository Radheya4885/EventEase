---
description: How to push code changes to GitHub for collaboration
---

# Pushing Changes to GitHub

// turbo-all

1. Check which files have changed:
```
git status
```

2. Stage all changes:
```
git add .
```

3. Commit with a descriptive message:
```
git commit -m "Describe what you changed here"
```

4. Push to GitHub:
```
git push origin main
```

5. Tell your collaborator to pull the latest changes with `git pull`.
